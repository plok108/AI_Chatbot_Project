# 냉털이 — 논문화를 위한 추가 기능 로드맵 (쉽게 풀어 쓴 버전)

> 작성일: 2026-06-02
> 목적: 학교 프로젝트를 **시연**하고, 그것을 바탕으로 **논문**을 쓰기 위함
> 읽는 사람: 개발을 잘 모르는 사람도 이해할 수 있도록 작성

---

## 0. 이 문서가 말하려는 것 (30초 요약)

지금 만든 "냉털이"는 잘 작동하지만, 속을 들여다보면 **거의 모든 판단을 GPT(챗봇)에게 맡기고** 있습니다.
즉 "AI한테 잘 물어보는 앱"에 가깝습니다.

그런데 논문이 되려면 **"우리가 직접 만든 무언가"가 있어야** 합니다. 단순히 "GPT를 불렀습니다"는
논문의 기여(=새로운 점)가 되기 어렵습니다.

그래서 이 문서는 **"무엇을 더 추가하면 논문에 쓸 거리가 생기는가"**를, 쉬운 설명과 예시로 정리합니다.

---

## 0.5 구현 현황 (2026-06-02 기준 — ver5·ver6 반영)

아래 표의 항목은 **이미 코드로 구현되어 동작**합니다. 즉, 위 "빈칸"의 핵심이 채워졌습니다.
(자세한 변경 이력은 `CHANGES_ver5.md`=하이브리드 파이프라인, `CHANGES_ver6.md`=정밀화·리팩토링)

| 기능 | 상태 | 어디에 |
|------|:----:|--------|
| **T1-1 하이브리드 재정렬** (코드가 순위 재결정) | 구현 (ver5) | `app/services/scoring_service.py` |
| **T1-2 안전 필터** — 이름 매칭 | 구현 (ver5) | `app/services/safety_service.py` |
| **T1-2 안전 필터** — 다층 강화(재료+숨은양념 규칙표+검증불가 경고+면책) | 구현 (ver6) | `safety_service.py` + `ChatPage.jsx` |
| **T1-3 상황 인식** — 시간 | 디바이스(브라우저) 시각 (ver6) | `context_service.py` + `ChatPage.jsx` |
| **T1-3 상황 인식** — 날씨 | 사용자 수동 입력 (ver6) / 무료 API는 키 넣으면 자동 | `context_service.py` + `ChatPage.jsx` |
| **상세 레시피** (분량·단계별 불세기·대체재료 등) | 구현 (ver6) | `prompt_service.build_recipe_prompt` |
| **T1-4 실험용 on/off 스위치(ablation)** | 구현 (ver5) | `app/config.py` (`ENABLE_*`, `WEIGHT_*`) |
| **T1-4 구조화 로깅 + 지표 자동 계산** | 구현 | `recommendation_events` 테이블 + `backend/metrics.py` |
| T2/T3 (영양·선호고도화·RAG 등) | 예정 | — |
| (기반) JWT 인증·보안 | 구현 (ver4) | `CHANGES_ver4.md` |

> 핵심: 이제 추천은 **"GPT가 후보 생성 → 우리 코드가 안전 필터 + 재정렬"**의 하이브리드 구조로 동작합니다.
> 상황(시간·날씨)은 디바이스/사용자에게서 받고, 알러지 안전은 여러 겹으로 막되 면책을 명시합니다.
> `ENABLE_*` 값을 모두 `false`로 두면 **순수 GPT(baseline)**, 모두 `true`면 **제안 시스템**이 되어
> 그대로 ablation 실험에 쓸 수 있습니다. (전체 흐름은 9장 참고)

---

## 1. 먼저 알아두면 좋은 용어 (아주 쉽게)

| 용어 | 쉬운 뜻 | 비유 |
|------|--------|------|
| **LLM / GPT** | 사람처럼 글을 써주는 AI. 똑똑하지만 가끔 틀리고, 매번 답이 조금씩 달라짐 | 아는 게 많지만 변덕스러운 친구 |
| **규칙 기반** | 사람이 정한 명확한 규칙대로 컴퓨터가 계산하는 방식. 항상 같은 입력엔 같은 결과 | 계산기처럼 정확한 도구 |
| **하이브리드** | 위 둘을 **합친 것**. AI가 아이디어를 내고, 규칙이 검토·정리 | 친구가 후보를 말하면, 내가 체크리스트로 거르는 것 |
| **baseline(기준선)** | 비교의 기준이 되는 "기본 버전". 보통 "아무 기능 없이 GPT만 쓴 버전" | 시험에서 비교용 대조군 |
| **ablation(제거 실험)** | 기능을 하나씩 껐다 켜보며 "이 기능이 진짜 효과 있나" 확인하는 실험 | 요리에서 재료 하나 빼보고 맛 비교 |
| **지표(metric)** | 성능을 숫자로 잰 것 (예: 정확도 80%) | 시험 점수 |
| **정량 평가** | 느낌이 아니라 **숫자로** 증명하는 것 | "맛있다" 대신 "10명 중 9명이 선택" |

이 단어들이 논문 "실험" 부분의 핵심입니다.

---

## 2. 지금 시스템이 실제로 하는 일 (예시로 이해하기)

**상황 예시:** 사용자가 냉장고에 `달걀, 김치, 대파`가 있고, `새우 알러지`가 있으며,
"비 오는 추운 저녁에 뭐 먹지?"라고 물었다고 합시다.

현재 냉털이의 동작:

```
1. 사용자가 입력 ────────────────┐
2. 코드가 재료·기피음식을 모아 GPT에게 보낼 "지시문(프롬프트)"을 만든다
3. GPT가 알아서 메뉴 2~3개를 만들고, 활용률·비용·순서까지 "스스로" 정해서 돌려준다
4. 그 결과를 화면 카드로 보여준다
5. 사용자가 고른 메뉴를 기록한다 → 다음에 비슷한 걸 더 추천(선호 학습)
```

**여기서 중요한 점:** 2~5번 중 "지능"에 해당하는 거의 전부(메뉴 고르기, 순위, 활용률 계산,
알러지 제외)를 **GPT가 합니다.** 우리 코드는 "잘 물어보고, 결과를 화면에 그리는" 역할에 가깝습니다.

유일하게 우리가 직접 만든 알고리즘은 **"선호 학습"**(사용자가 매운 걸 자주 고르면 매운 걸 더 추천)
하나인데, 이것도 단어 비교 수준의 단순한 방식입니다.

---

## 3. 문제: "한다고 적어둔 것"과 "실제로 하는 것"이 다르다

기획 문서(project-improvements.md)에는 멋진 기능들이 적혀 있지만, 코드에는 없는 것이 많습니다.
논문 심사자가 문서와 코드를 비교하면 바로 들통나는 부분입니다.

아래 표는 **이번 작업 전(과거)** 기준의 격차이며, 오른쪽에 **현재 상태**를 함께 표시했습니다.

| 적어둔 것 | (과거) 실제 코드 | **현재 상태** |
|-----------|----------|---------|
| "규칙으로 점수 매겨 추천" | 없음 (GPT가 다 함) | 코드 재정렬 구현(scoring_service) |
| "GPT + 규칙 결합(하이브리드)" | 없음 (GPT 단독) | 후보 생성=GPT, 정렬/필터=코드 |
| "날씨 반영" | 없음 (날씨를 받지도 않음) | 사용자 수동 입력(ver6) / API는 옵션 |
| "시간대별 패턴(야식 등)" | 없음 | 디바이스 시각 사용(ver6) |
| "영양 분석" | 이름표만 있음 | 예정(T2-2) |
| "알러지 자동 제외" | GPT한테 부탁만 함 | 다층(이름+재료+숨은양념 규칙표)+면책(ver6) |

> 핵심 메시지: 위 빈칸의 **대부분이 이번 작업으로 채워졌습니다.** 남은 빈칸(영양 등)이 곧 다음 논문 거리입니다.
> 아래 4장은 각 기능을 쉽게 설명하고, **9장에 현재 전체 흐름**을 정리했습니다.

---

## 4. 추가하면 논문이 되는 기능들

각 기능을 **① 무엇인지 ② 예시 ③ 지금과 뭐가 다른지 ④ 논문에 왜 좋은지 ⑤ 어떻게 숫자로 증명하는지 ⑥ 난이도** 순으로 설명합니다.

---

### Tier 1 — 가장 중요 (논문의 뼈대. 이것부터 권장)

#### T1-1. "하이브리드 재정렬" — AI가 후보를 내고, 우리 코드가 순위를 정한다

- **① 무엇인지**: 지금은 GPT가 메뉴 순서까지 정합니다. 이걸 바꿔서, **GPT는 후보만 내고
  우리 코드가 점수를 매겨 순위를 다시 정합니다.**
  점수 = (재료 활용률 × 가중치) + (취향 일치 × 가중치) + (날씨·시간 적합 × 가중치) + (비용 효율 × 가중치)
- **② 예시**: GPT가 `김치찌개, 계란말이, 새우볶음밥`을 후보로 냈다면, 우리 코드가
  "달걀·김치·대파를 잘 쓰고(활용률 높음), 추운 날에 맞는(국물)" 김치찌개에 높은 점수를 줘서 1등으로 올립니다.
- **③ 지금과 다른 점**: 순위 결정권이 **변덕스러운 GPT → 일관된 코드**로 넘어옵니다.
  같은 상황이면 항상 같은 순위가 나와 **재현 가능**해집니다(논문에서 매우 중요).
- **④ 논문에 왜 좋은지**: 이것이 바로 **"AI + 규칙 하이브리드"**라는, 제목과 일치하는 핵심 기여입니다.
  "GPT만 쓴 버전"과 "우리 코드가 재정렬한 버전"을 비교하는 실험을 만들 수 있습니다.
- **⑤ 어떻게 증명하나**: 사용자가 실제로 1~2등 메뉴를 더 자주 고르는지(상위 선택률),
  평균 재료 활용률이 오르는지를 숫자로 비교.
- **⑥ 난이도**: 중 (반나절~하루). 기존 추천 코드 뒤에 "점수 계산 한 단계"를 끼워 넣는 일.

> **구체 예시 — 점수로 순위가 어떻게 바뀌나**
>
> 점수 비중을 이렇게 정했다고 합시다: 활용률 40% · 취향 30% · 상황(날씨/시간) 20% · 비용효율 10%.
> (사용자는 "국물 음식"을 자주 골랐고, 지금은 추운 비 오는 저녁)
>
> | 후보(GPT가 냄) | 활용률 | 취향 | 상황 | 비용효율 | **최종 점수** |
> |---|:---:|:---:|:---:|:---:|:---:|
> | 김치찌개 | 80 | 100 | 100 | 90 | **0.4×80+0.3×100+0.2×100+0.1×90 = 91** |
> | 라면 | 50 | 70 | 80 | 95 | 0.4×50+0.3×70+0.2×80+0.1×95 = 66.5 |
> | 계란말이 | 90 | 40 | 30 | 100 | 0.4×90+0.3×40+0.2×30+0.1×100 = 64 |
>
> - **GPT가 준 원래 순서:** 계란말이 → 라면 → 김치찌개 (GPT 마음대로)
> - **우리 코드가 다시 정한 순서:** 김치찌개(91) → 라면(66.5) → 계란말이(64)
>
> 추운 날 + 국물 취향이라는 맥락이 점수에 반영되어 **김치찌개가 꼴찌에서 1등으로** 올라왔습니다.
> 그리고 같은 입력이면 **항상 똑같은 순위**가 나옵니다(재현 가능).

#### T1-2. "안전 필터" — 알러지·기피음식을 코드가 강제로 제거 (가장 가성비 좋음)

- **① 무엇인지**: 지금은 GPT에게 "새우 빼줘"라고 **부탁**만 합니다. 이걸 바꿔서,
  GPT가 새우 메뉴를 내더라도 **우리 코드가 무조건 걸러서 버립니다.**
- **② 예시**: GPT가 실수로 `새우볶음밥`을 추천해도, 코드가 "새우=알러지"임을 알고
  화면에 나가기 전에 삭제합니다. 사용자는 절대 새우 메뉴를 보지 않습니다.
- **③ 지금과 다른 점**: "AI가 가끔 실수함" → "코드가 100% 막음"으로 바뀝니다.
- **④ 논문에 왜 좋은지**: **"위반율을 0%로 만들었다"**는 결과는 표 한 줄로 아주 강력합니다.
  "AI한테 부탁만 한 버전은 가끔 알러지 음식이 새어 나왔지만(예: 5%), 우리 필터는 0%"처럼
  보여주기 깔끔한 대표 결과가 됩니다.
- **⑤ 어떻게 증명하나**: 여러 번 추천을 돌려서 **기피음식이 결과에 섞인 비율**을 두 버전(부탁만 vs 코드필터)으로 비교.
- **⑥ 난이도**: 하 (2~3시간). 단, "새우 = 칵테일새우·대하·쉬림프…" 같은 **단어 사전**이 조금 필요.
  → 처음엔 간단한 사전으로 시작하고 점점 늘리면 됩니다.

> **구체 예시 — 필터가 걸러내는 모습**
>
> 사용자 알러지: `새우`, 기피: `오이`. 코드가 가진 단어 사전(일부):
> ```
> 새우 → [새우, 대하, 칵테일새우, 깐새우, 쉬림프, 새우젓]
> 오이 → [오이, 오이무침, 오이냉국]
> ```
> GPT가 추천 4개를 냈을 때:
>
> | GPT 추천 | 코드 판정 | 이유 |
> |---|---|---|
> | 김치찌개 | 통과 | 위험 단어 없음 |
> | 깐새우볶음밥 | 제거 | "깐새우" = 새우 사전에 걸림 |
> | 오이무침 비빔밥 | 제거 | "오이무침" = 오이 사전에 걸림 |
> | 계란말이 | 통과 | 위험 단어 없음 |
>
> → **사용자에게는 김치찌개·계란말이 2개만** 보입니다. GPT가 실수로 새우를 넣어도 화면에 절대 안 나갑니다.
> 논문에는 "부탁만 한 버전은 알러지 음식이 6% 새어 나왔지만, 코드 필터는 0%"로 적습니다.

#### T1-3. "자동 상황 인식" — 날씨와 시간을 스스로 가져온다

- **① 무엇인지**: 지금 "상황"은 사용자가 직접 타이핑한 글자 한 줄뿐입니다.
  여기에 **현재 날씨(무료 날씨 API)와 시각**을 자동으로 받아 추천에 반영합니다.
- **② 예시**: 사용자가 아무 말 안 해도, 앱이 "지금 4℃, 비, 저녁 8시"임을 알아서
  따뜻한 국물 메뉴나 야식에 어울리는 메뉴 점수를 올립니다.
- **③ 지금과 다른 점**: 사용자가 일일이 설명 → 앱이 **알아서 파악**. 진짜 "상황 인식".
- **④ 논문에 왜 좋은지**: 프로젝트 **제목이 "상황 인식 기반"인데 지금은 그게 비어 있습니다.**
  이걸 채우면 제목과 내용이 일치하고, "자동 맥락 수집"이라는 기여가 생깁니다.
- **⑤ 어떻게 증명하나**: 추운 날 따뜻한 메뉴 비율이 실제로 오르는지, 사용자가 "상황에 맞다"고
  느끼는지(간단 설문)로 평가.
- **⑥ 난이도**: 중 (반나절). 날씨 API 키 발급 + 도시명 입력 + T1-1 점수에 연결.

> **구체 예시 — 날씨만 바뀌어도 추천이 달라진다**
>
> 똑같은 사용자, 똑같은 냉장고(`달걀, 김치, 대파, 소면`)인데 날씨만 다를 때:
>
> | 상황(자동 인식) | 1등 추천 | 왜 |
> |---|---|---|
> | 4℃, 비, 저녁 8시 | **김치찌개** | 추운 날 → 따뜻한 국물 점수↑, 저녁 → 든든한 메뉴 |
> | 31℃, 맑음, 낮 1시 | **김치말이국수** | 더운 날 → 시원한 면 점수↑ |
>
> 사용자는 날씨를 한 글자도 입력하지 않았는데, 앱이 알아서 파악해 추천을 바꿉니다.
> 이게 프로젝트 제목 "**상황 인식**"의 실제 모습입니다.

#### T1-4. "평가 체계" — 결과를 숫자로 만드는 장치 (논문의 척추)

- **① 무엇인지**: 위 기능들이 "정말 더 나은지"를 **숫자로 측정하고 비교**하는 틀.
  실험을 위해 기능을 껐다 켜는 스위치, 지표를 자동 계산하는 작은 프로그램을 만듭니다.
- **② 좋은 소식**: 평가에 필요한 **데이터는 이미 쌓이고 있습니다.**
  - `chat_logs` = 우리가 추천한 기록
  - `user_food_history` = 사용자가 실제로 고른 기록
  여기서 바로 여러 지표를 뽑을 수 있습니다.
- **③ 뽑을 수 있는 숫자(지표)**:
  - **추천 수용률** = 사용자가 고른 횟수 ÷ 추천한 횟수 (높을수록 추천이 마음에 들었다는 뜻)
  - **평균 재료 활용률** (냉장고 재료를 얼마나 잘 썼나)
  - **기피음식 위반율** (알러지·싫은 음식이 섞인 비율 — 0%가 목표)
  - **개인화 효과** (쓸수록 사용자 취향에 더 잘 맞아가는지)
  - **응답 시간 / 비용** (빠른지, AI 호출 비용은 얼마인지)
  - (가능하면) **만족도 설문** (사용자에게 5점 만점으로 물어보기)
- **④ 논문에 왜 좋은지**: 논문의 "실험·결과" 장은 **전부 이 숫자들로 채워집니다.** 이게 없으면 논문이 안 됩니다.
- **⑥ 난이도**: 중 (하루). 지표 계산 스크립트 + 기능 on/off 스위치.

> **구체 예시 — 쌓인 기록에서 숫자를 뽑는 법**
>
> DB에 이런 기록이 있다고 합시다(가상):
> - `chat_logs`에 저장된 **추천 횟수: 10번**, 추천된 메뉴는 총 25개
> - `user_food_history`에 저장된 **사용자가 실제로 고른 횟수: 6번**
> - 추천된 25개 메뉴 중 기피음식이 섞여 나온 메뉴: 1개
>
> 그러면 지표는 이렇게 계산됩니다:
> ```
> 추천 수용률 = 6 ÷ 10 = 60% (10번 추천 중 6번은 마음에 들어 골랐다)
> 기피음식 위반율 = 1 ÷ 25 = 4% (목표는 0%)
> 평균 재료 활용률 = (각 메뉴 활용률의 평균) = 예: 72%
> ```
> 이 숫자들을 "기능 끈 버전"과 "기능 켠 버전"에서 각각 뽑아 비교하면 그게 곧 논문 결과표입니다.

---

### Tier 2 — 차별화 요소 (여유가 있으면 추가)

#### T2-1. 취향 학습을 더 똑똑하게
- **무엇/예시**: 지금은 "매운 음식 단어가 몇 번 나왔나"만 셉니다. 이를 개선해
  ① **최근에 고른 것에 더 큰 비중**, ② **아침/야식 등 시간대별로 취향 구분**,
  ③ 단어가 조금 달라도 같은 뜻으로 인식(예: "제육"과 "두루치기")하게 만듭니다.
- **논문 가치**: "사용할수록 똑똑해지는 개인화"를 더 설득력 있게. 처음 쓰는 사용자(데이터 없음) 대응도 논의 가능.
- **난이도**: 중.

#### T2-2. 진짜 영양 분석 (한국 공공 데이터 활용)
- **무엇/예시**: 식약처가 무료로 공개한 **식품영양성분 데이터**를 연결해, 각 메뉴의
  대략적인 칼로리·단백질을 계산합니다. "다이어트 중" 사용자에겐 저칼로리, "근육" 목표엔 고단백을 우선.
- **논문 가치**: "건강식"이 이름표에서 **실제 영양 기준 추천**으로 격상. 공개 데이터라 누구나 재현 가능(논문에서 좋아함).
- **난이도**: 중~상 (메뉴와 영양 데이터를 연결하는 작업이 핵심).

#### T2-3. 예산을 고려한 추천
- **무엇/예시**: 이미 계산하는 "추가 비용"을 활용해, "5천 원 이하"처럼 예산 한도 안에서 가장 좋은 메뉴를 추천.
- **논문 가치**: 자취생 시나리오에 딱 맞는 "예산 인식 추천". 비용 대비 활용률 그래프를 그릴 수 있음.
- **난이도**: 하~중.

---

### Tier 3 — 야심찬 옵션 (시간이 정말 충분할 때만)

#### T3-1. 레시피 출처 근거 달기 (환각 줄이기)
- **무엇**: GPT가 레시피를 지어낼 때(=환각) 실제 레시피 데이터에서 찾아 **근거에 맞춰** 답하게 함.
- **논문 가치**: "AI가 거짓말하는 문제를 줄였다"는 주제. 단, 구축 부담이 큼.
- **난이도**: 상.

#### T3-2. 본격 사용자 비교 실험
- **무엇**: 어느 쪽이 우리 버전인지 모르게 한 채(블라인드) 두 버전을 사용자에게 보여주고 선호를 조사.
- **논문 가치**: 사람이 직접 평가한 결과는 설득력이 큼. 다만 참여자 수를 모으는 게 관건.
- **난이도**: 중 (사람 모으기가 핵심).

---

## 4.5 처음부터 끝까지 따라가는 종합 예시 (모든 기능이 함께 동작)

한 사용자의 요청이 **개선된 시스템 전체**를 통과하는 과정을 따라가 봅시다.

**등장인물:** 자취생 민수
- 냉장고: `달걀, 김치, 대파, 두부, 소면`
- 알러지/기피: `새우`, `오이`
- 자주 고른 메뉴 기록: 김치찌개, 부대찌개, 라면 → 시스템이 "**국물 음식 선호**"로 학습함
- 지금: 앱이 자동 감지 → `4℃, 비, 저녁 7시`
- 입력: "오늘 너무 춥다. 간단하게 뭐 먹지?"

```
[1단계] 입력 수집
   민수의 재료 + 기피음식 + 학습된 취향(국물 선호) + 자동 날씨/시간을 모은다

[2단계] GPT가 후보 생성
   GPT: "계란말이, 김치찌개, 새우두부조림, 오이냉국비빔밥" ← 4개 제안

[3단계] 안전 필터(T1-2)가 위험 메뉴 제거
   - 새우두부조림 → "새우" 걸림 삭제
   - 오이냉국비빔밥 → "오이" 걸림 삭제
   남은 후보: 계란말이, 김치찌개

[4단계] 하이브리드 재정렬(T1-1) + 상황 인식(T1-3)
   각 후보에 점수를 매김 (추운 비 오는 저녁 + 국물 선호 반영)
   - 김치찌개 : 활용률 80, 취향 100(국물), 상황 100(추움) → 91점
   - 계란말이 : 활용률 90, 취향 40, 상황 30 → 64점
   순위: ① 김치찌개 ② 계란말이

[5단계] 화면 표시
   민수는 "김치찌개(활용률 80%, 추가비용 0원)"를 1순위로 본다
   → 새우·오이는 아예 보이지 않음, 추운 날씨에 맞는 국물 메뉴가 위에

[6단계] 민수가 김치찌개 선택 → 기록 저장
   → 다음에 "국물 선호"가 더 강해지고, 평가용 데이터(추천 1건·선택 1건)도 쌓임
```

**같은 요청을 "기준선(GPT만)" 버전에 넣으면?**
→ 안전 필터가 없어 새우두부조림이 그대로 추천될 수 있고(알러지 위험!),
   순위도 GPT 기분대로라 추운 날인데 계란말이가 1등으로 나올 수 있습니다.

**이 한 번의 비교가 곧 논문의 핵심 그림 한 장**이 됩니다.

---

## 5. 논문의 "결과"는 어떻게 만들어지나 (실험 개념 쉽게)

논문에서 가장 중요한 건 **"우리 게 더 낫다"를 숫자로 보여주는 표**입니다. 만드는 방법:

```
① 두 가지(또는 그 이상) 버전을 준비한다
   - A: 기준선(baseline) = 아무 기능 없이 GPT만 쓴 버전
   - B: 우리 제안 = 안전필터 + 하이브리드 + 상황인식까지 켠 버전

② 같은 상황 묶음(예: 30~50가지 시나리오)을 두 버전에 똑같이 넣는다

③ 각 버전의 지표를 잰다
```

예상되는 결과 표(가상의 예시):

| 지표 | A. 기준선(GPT만) | B. 우리 제안 |
|------|:---:|:---:|
| 기피음식 위반율 | 6% | **0%** |
| 평균 재료 활용률 | 61% | **78%** |
| 추천 수용률 | 44% | **63%** |
| 평균 응답 시간 | 1.2초 | 1.5초 |

여기에 **ablation(제거 실험)**을 더하면 더 좋습니다:
"안전필터만 켰을 때", "거기에 하이브리드까지 켰을 때"를 단계별로 보여주면
**어느 기능이 얼마나 기여했는지**가 드러납니다.

---

## 6. 추천 진행 순서 (시간 대비 효율)

| 시나리오 | 무엇을 만드나 | 결과 |
|---------|--------------|------|
| **최소(가성비)** | T1-2 안전필터 + T1-4 평가 | "위반율 0% + 수용률 개선" 표 하나로 논문 골격 완성 |
| **권장** | T1-1 + T1-2 + T1-3 + T1-4 | 하이브리드·상황인식·평가 완비. 제목과 내용이 일치 |
| **풀** | 권장 + T2-1 + T2-2 | 개인화·영양까지. 새로운 점(기여)이 3~4개 → 분량 충분 |

> 가장 먼저 **T1-2(안전필터)**를 권합니다. 작업량이 적은데 **"위반율 0%"**라는
> 보여주기 좋은 대표 결과가 빨리 나와서, 논문 실험 장을 일찍 채울 수 있습니다.

---

## 7. 논문 목차에 어떻게 들어가나 (어디에 무엇을 쓸지)

| 논문 장 | 여기에 들어갈 내용 |
|---------|------------------|
| 1. 서론 | 자취생의 "오늘 뭐 먹지" 문제, 기존 추천의 한계 |
| 2. 관련 연구 | 기존 음식 추천, LLM 기반 추천, 규칙 기반 추천 |
| 3. 제안 시스템 | 전체 구조 그림 + 하이브리드(T1-1)·안전필터(T1-2)·상황인식(T1-3) 설명 |
| 4. 구현 | 사용 기술(React/FastAPI/PostgreSQL/GPT), JWT 인증 등은 여기 한 문단 |
| 5. 실험 | 평가 방법(T1-4), 지표 정의, baseline·ablation 설계 |
| 6. 결과 및 분석 | 위 5장의 숫자 표 + 해석 |
| 7. 결론 | 기여 요약, 한계, 향후 과제(Tier 2~3) |

> 참고: 앞서 한 **JWT 인증·보안 작업**은 4장(구현)의 한 문단으로 들어갑니다.
> 중요한 엔지니어링이지만 "새로운 연구 결과"는 아니라서 핵심 기여로는 쓰지 않습니다.

---

## 8. 데모(시연) 마감용 — 논문과 별개지만 사고 방지

시연 중 "미완성처럼 보이는" 부분을 막는 작업들입니다(논문 가치는 낮음).

- 설정 화면의 "준비 중" 버튼(닉네임·비밀번호 변경) → 만들거나 숨기기
- 메뉴를 빠르게 두 번 눌렀을 때 중복 추천되는 것 방지
- 실패 알림창(`alert`) → 화면 안 메시지로 (다크모드와 어울리게)
- 다크모드 설정이 새로고침해도 유지되게
- 안 쓰는 코드 정리

---

## 9. 전체 프로젝트 흐름 (현재 시스템 구조)

이번 작업까지 반영된 **실제 동작 흐름**입니다. 논문 3장(제안 시스템)의 구조도로 그대로 쓸 수 있습니다.

### 9-1. 큰 그림 (3단 구조)

```
 [사용자 브라우저] [백엔드 서버] [외부/저장소]
  React + Vite FastAPI (Python)
  ─────────────── ──────────────────────── ─────────────
  · 로그인/채팅 화면 ──HTTP──▶ · JWT로 사용자 신원 확인 ──────▶ PostgreSQL
  · Authorization · 추천 파이프라인 실행 (사용자·재료·
    : Bearer 토큰 · 결과 저장/조회 기피음식·기록)
  · 게스트는 토큰 없음 ◀──JSON── · ... ──────▶ OpenAI GPT
                                                                   OpenWeather(선택)
```

### 9-2. 추천 한 번이 처리되는 순서 (핵심 파이프라인)

사용자가 "오늘 뭐 먹지?"를 보냈을 때, 백엔드 `POST /api/chat/recommend`가 하는 일:

```
요청 도착
  │
  ① 신원 확인 (dependencies.py)
  │ 토큰 있으면 → user_id, 없으면 → 게스트(None)
  │
  ② 입력 수집
  │ · 냉장고 재료 (로그인: DB에서 자동 로드 / 게스트: 요청값)
  │ · 기피음식 (요청값 + 로그인 시 DB값 합침)
  │ · 선호 카테고리 (preference_service: 과거 선택 기록 분석)
  │ · 현재 상황 (context_service: 디바이스 시각 + 사용자 수동 날씨) ← T1-3 (ver6)
  │
  ③ 프롬프트 생성 (prompt_service) → GPT 호출 (ai_service, JSON 모드)
  │ GPT가 메뉴 후보 2~3개를 생성 (이름·이유·활용률·추가비용·전체 재료) ← 재료 펼치기(ver6)
  │
  ④ 안전 필터 (safety_service) ← T1-2 [ENABLE_SAFETY_FILTER]
  │ 이름 + 전체 재료 + 숨은 양념 규칙표(새우젓→갑각류 등)로 기피·알러지 후보 제거 (ver6)
  │ 재료 검증 불가 후보는 제거 대신 경고(safety_note) — '애매하면 알린다'
  │
  ⑤ 규칙 기반 재정렬 (scoring_service) ← T1-1 [ENABLE_RERANK]
  │ 점수 = 0.4·활용률 + 0.3·취향 + 0.2·상황 + 0.1·비용효율
  │ 점수 내림차순으로 순위 재결정 (같은 입력 → 항상 같은 순위)
  │
  ⑥ 저장 & 응답
       chat_logs에 기록 → 후보 목록(재료·경고 포함) + 면책 문구를 화면으로 반환
```

> 2단계 추천(선택 → 레시피)은 ver2 흐름 유지. 단, 레시피는 ver6에서 더 상세해짐
> (분량·사전준비·단계별 불세기/시간·대체재료·실수·보관).
> 사용자가 카드를 고르면 `POST /api/chat/select`로 선택을 저장하고(선호 학습 데이터),
> `POST /api/chat/recipe`로 그 메뉴의 상세 레시피만 생성합니다.
> ※ 안전 필터는 위험을 줄이는 보조 수단이며 **의학적 안전을 보장하지 않습니다**(화면에 면책 표시).

### 9-3. 백엔드 폴더 구조 (이번에 추가된 파일 )

```
backend/app/
├── config.py 기능 스위치·가중치·키 설정 (확장)
├── dependencies.py JWT 인증 의존성 (ver4)
├── database.py DB 연결·CRUD
├── models/schemas.py 요청/응답 형식
├── routers/
│ ├── auth.py 로그인·회원가입(토큰 발급)
│ ├── chat.py 추천 파이프라인 (②~⑥ 통합)
│ ├── ingredients.py 냉장고 재료
│ └── preferences.py 기피음식
└── services/
    ├── ai_service.py GPT 호출
    ├── prompt_service.py 프롬프트 생성 (상황 정보 + 재료 펼치기 + 상세 레시피)
    ├── preference_service.py 선호 분석 (카테고리 분류 추가)
    ├── token_service.py JWT 발급/검증 (ver4)
    ├── context_service.py 상황 인식 (ver6: 디바이스 시간·수동 날씨, 상수화)
    ├── safety_service.py 안전 필터 (ver6: 재료·숨은양념 다층, 경고)
    └── scoring_service.py 규칙 재정렬 (ver5, ver6 리팩토링)
```

> ver6 리팩토링: 흩어진 매직 넘버(점수 50/40/100, 온도 12/28, 시간대 경계, 비용 기준 5000원 등)를
> `scoring_service`·`context_service`·`safety_service`의 **이름 있는 상수**로 정리해 대학생도 읽기 쉽게 했습니다.

### 9-4. 실험(ablation)을 돌리는 법 — `.env` 스위치만 바꾸면 됨

같은 코드로 baseline과 제안 시스템을 전환할 수 있어, 별도 분기 없이 비교 실험이 가능합니다.

| 실험 버전 | ENABLE_SAFETY_FILTER | ENABLE_RERANK | ENABLE_CONTEXT |
|-----------|:---:|:---:|:---:|
| A. 기준선(순수 GPT) | false | false | false |
| B1. +안전필터만 | **true** | false | false |
| B2. +재정렬까지 | **true** | **true** | false |
| B3. 제안 전체 | **true** | **true** | **true** |

> 각 버전으로 같은 시나리오 묶음을 돌려 지표(위반율·활용률·수용률)를 재면 5장의 결과표가 만들어집니다.
> 점수 비중도 `WEIGHT_*`로 조절해 민감도 분석을 추가할 수 있습니다.

---

## 한 줄 결론

> **"GPT한테 잘 물어보는 앱"에, 우리가 직접 만든 "규칙(다층 안전필터·재정렬)"과
> "상황 인식(디바이스 시간·사용자 날씨)"을 더하고, 레시피도 자세하게 만들었습니다(ver5·ver6 구현 완료).
> 이제 남은 일은 그 효과를 숫자로 증명하는 평가(T1-4)뿐이고, 그게 곧 논문이 됩니다.**
