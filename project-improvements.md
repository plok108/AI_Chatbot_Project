# 상황 인식 기반 AI 식사 추천 챗봇

## 프로젝트 개요

기존 프로젝트는 단순히 GPT API를 활용하여 음식 추천을 제공하는 일반적인 AI 챗봇 구조에 가까웠다.
하지만 실제 사용자 환경에서는 단순 음식 추천뿐만 아니라 사용자의 상황, 보유 재료, 음식 취향, 알러지 정보 등을 종합적으로 고려한 추천 서비스가 필요하다고 판단하였다.

이에 따라 프로젝트를 단순 질의응답형 챗봇이 아닌, 사용자 상황을 분석하여 개인 맞춤형 식사를 추천하는 형태로 개선하였다.

---

# 프로젝트명 변경

기존 프로젝트명은 단순 식사 여부를 판단하는 느낌이 강하였기 때문에 프로젝트의 핵심 기능을 보다 명확하게 표현할 수 있도록 프로젝트명을 변경하였다.

## 변경 전

- 식사 의사결정 AI 챗봇

## 변경 후

- 상황 인식 기반 AI 식사 추천 챗봇

해당 제목은 사용자의 현재 상황과 환경을 분석하여 맞춤형 식사를 추천하는 프로젝트의 특징을 강조하기 위해 선정하였다.

---

# 기존 프로젝트의 문제점

기존 프로젝트는 단순히 사용자 입력을 GPT에게 전달하여 응답을 생성하는 일반적인 AI 챗봇 구조였다.

또한 아래와 같은 문제점이 존재하였다.

- 사용자의 알러지 및 음식 제한 사항 반영 부족
- 현재 날씨 및 사용자 상태 미반영
- 냉장고 보유 재료 활용 기능 부족
- 사용자 취향 분석 기능 부재
- 단순 GPT 응답 의존 구조
- 일반적인 음식 추천 챗봇과의 차별성 부족

이를 해결하기 위해 사용자 상황 기반 추천 기능과 규칙 기반 추천 시스템을 추가하여 프로젝트를 개선하였다.

---

# 주요 개선 사항

## 1. 사용자 상황 기반 프롬프트 개선

기존에는 단순한 사용자 입력만을 활용하여 GPT에게 음식 추천을 요청하였다.

### 기존 방식

```python
prompt = f"""
사용자 재료: {ingredients}
현재 상황: {situation}
현재 기분: {mood}
요청: {request.user_message}

최적의 메뉴를 추천해줘.
"""
```

해당 방식은 사용자의 세부적인 조건을 충분히 반영하지 못하는 한계가 존재하였다.

이를 개선하기 위해 사용자별 상황 데이터를 구조화하여 동적으로 프롬프트를 생성하도록 변경하였다.

### 개선 후

```python
prompt = f"""
너는 개인 맞춤형 식사 추천 AI이다.

[사용자 정보]
알러지: {allergy}
못 먹는 음식: {disliked_food}
현재 날씨: {weather}
현재 기분: {mood}
사용 가능 재료: {ingredients}
목표: {diet_goal}

[규칙]
1. 알러지 음식은 절대 추천하지 마라.
2. 못 먹는 음식은 제외하라.
3. 날씨와 기분을 고려하라.
4. 조리 시간은 20분 이하로 추천하라.
5. 음식 추천과 간단 레시피를 함께 제공하라.
"""
```

또한 사용자가 일부 정보를 입력하지 않은 경우에도 정상적으로 동작할 수 있도록 조건문 기반의 동적 프롬프트 구조를 적용하였다.

---

## 2. 알러지 및 음식 제한 필터링 기능 추가

사용자의 알러지 정보 및 못 먹는 음식을 데이터베이스에 저장하여 추천 과정에서 자동으로 제외되도록 구현하였다.

이를 통해 사용자는 매번 알러지 정보를 입력하지 않아도 되며, 개인 맞춤형 추천이 가능하도록 개선하였다.

### 적용 예시

- 갑각류 알러지 사용자의 경우 갑각류 포함 음식 자동 제외
- 오이, 가지 등 기피 음식 자동 제외
- 특정 재료 포함 음식 추천 차단

---

## 3. 냉장고 재료 기반 추천 시스템 추가

기존 프로젝트에서는 단순 음식 추천만 제공하였으나, 개선 이후에는 사용자가 보유한 냉장고 재료를 기반으로 추천을 진행하도록 변경하였다.

사용자가 저장해둔 재료 데이터를 자동으로 불러와 음식 추천에 활용하도록 구현하였다.

### 추가 기능

- 보유 재료 활용률 계산
- 부족한 재료 안내
- 예상 추가 비용 제공
- 현재 재료 기반 조리 가능 음식 우선 추천

### 예시

```text
김치찌개 추천

보유 재료 활용률: 80%

추가 필요 재료:
- 두부
- 대파

예상 추가 비용:
약 5,000원
```

이를 통해 사용자가 실제로 조리 가능한 메뉴를 추천받을 수 있도록 개선하였다.

---

## 4. 사용자 선호 패턴 분석 기능 추가

기존 프로젝트는 사용자 이전 기록을 반영하지 못하는 일회성 추천 구조였다.

이를 개선하기 위해 사용자의 선택 기록을 분석하여 개인별 선호 패턴을 반영하는 기능을 추가하였다.

### 분석 요소

- 자주 선택하는 음식 종류
- 시간대별 식사 패턴
- 영양 성향 분석
- 반복 선택 메뉴 분석

### 적용 예시

- 매운 음식 자주 선택 → 매운 음식 추천 우선순위 증가
- 야간 시간대 이용 빈도 높음 → 야식 메뉴 우선 추천
- 단백질 위주 메뉴 반복 선택 → 고단백 식단 추천 강화

이를 통해 사용자 맞춤형 추천 정확도를 향상시키도록 개선하였다.

---

## 5. 규칙 기반 추천 시스템 도입

기존 프로젝트는 GPT의 응답에만 의존하는 구조였다.

이를 개선하기 위해 추천 알고리즘 기반의 규칙 시스템을 추가하였다.

추천 과정에서 사용자의 상황 데이터를 기반으로 음식 점수를 계산하여 우선순위를 결정하도록 설계하였다.

### 반영 요소

- 보유 재료 활용 가능 여부
- 날씨 적합도
- 사용자 기분
- 다이어트 여부
- 조리 시간
- 사용자 선호 음식
- 알러지 포함 여부

### 예시

- 재료 활용 가능 → 점수 증가
- 현재 날씨와 어울리는 음식 → 점수 증가
- 사용자가 선호하는 음식 → 점수 증가
- 알러지 포함 음식 → 추천 제외

이를 통해 단순 생성형 챗봇이 아닌 상황 인식 기반 추천 시스템 형태로 프로젝트를 개선하였다.

---

# 최종 시스템 구조

기존 프로젝트는 단순한 AI 챗봇 형태에 가까웠지만, 개선 이후에는 다음과 같은 구조를 가지도록 변경하였다.

```text
사용자 입력
↓
사용자 정보 및 상황 분석
↓
알러지 및 음식 제한 필터링
↓
냉장고 재료 분석
↓
추천 알고리즘 기반 음식 후보 선정
↓
GPT 기반 자연어 응답 및 레시피 생성
```

이를 통해 단순 질의응답형 챗봇이 아닌, 사용자 상황을 반영하는 개인 맞춤형 AI 식사 추천 시스템 형태로 프로젝트를 개선하였다.

---

# 기대 효과

- 사용자 상황 기반 맞춤형 음식 추천 가능
- 실제 조리가 가능한 메뉴 추천 제공
- 사용자 취향 분석 기반 추천 정확도 향상
- 일반적인 AI 챗봇과 차별화된 서비스 구현
- GPT 기반 자연어 생성과 규칙 기반 추천 시스템 결합
- 사용자 경험(UX) 향상 및 실사용 가능성 강화