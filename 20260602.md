# 2026-06-02 작업 정리

> 냉털이(상황 인식 기반 AI 식사 추천) — 하루 작업 요약
> 범위: 백엔드(FastAPI) + 프론트엔드(React) + 문서

---

## A. 보안 강화 — JWT 인증 + 비밀번호 해시 (ver4)

- **JWT 인증 도입**: `user_id`를 요청 body/경로로 받던 것을 → **토큰에서만 추출**(타인 데이터 접근 차단)
- 보호 엔드포인트 경로에서 `user_id` 제거 (`/ingredients/{user_id}` → `/ingredients` 등), recommend/select/recipe는 게스트 허용
- 비밀번호 해시를 **자기서술적 포맷**(`pbkdf2_sha256$반복$salt$digest`)으로, 구버전 해시 하위호환 유지
- 로그인/회원가입 응답에 `access_token` 추가

## B. 규칙 기반 하이브리드 추천 파이프라인 (논문 핵심 기여)

- **안전 필터**(`safety_service`) — 기피·알러지를 코드가 강제 제거
- **재정렬**(`scoring_service`) — `0.4활용률 + 0.3취향 + 0.2상황 + 0.1비용`으로 코드가 순위 재결정
- **상황 인식**(`context_service`) — 시간대/날씨 반영
- **ablation 스위치**(`config.py`의 `ENABLE_*`, `WEIGHT_*`) — 다 끄면 baseline, 켜면 제안 시스템

## C. 정밀화 구현 (계획 parallel-stirring-unicorn)

1. **디바이스 시간**: 브라우저 `new Date().getHours()` 전송 → 서버가 UTC여도 정확
2. **수동 날씨**: 사용자가 추움/보통/더움 + 비·눈 직접 선택(무료 API는 키 넣으면 자동 전환)
3. **알러지 다층 방어**: GPT가 전체 재료 펼침 → 코드가 재료까지 검사 + **숨은 양념 규칙표**(새우젓→갑각류, 마요네즈→계란 등) + 검증 불가 시 경고 + **면책 문구**
4. **상세 레시피**: 분량·사전준비·단계별 불세기/시간·대체재료·실수·보관까지 확장

---

## 변경된 파일 목록

### 백엔드 — 신규(5)
- `app/dependencies.py` (인증 의존성)
- `app/services/token_service.py` (JWT)
- `app/services/context_service.py` (상황 인식)
- `app/services/safety_service.py` (안전 필터)
- `app/services/scoring_service.py` (재정렬)

### 백엔드 — 수정(10)
- `config.py` (JWT·기능플래그·가중치·날씨)
- `app/services/auth_service.py` (해시 포맷)
- `app/services/preference_service.py` (`classify_food`/`preferred_categories`)
- `app/services/prompt_service.py` (재료 요구 + 상세 레시피)
- `app/models/schemas.py` (AuthResponse, 입력길이, `client_hour`·날씨, `ingredients`·`safety_note`)
- `app/routers/auth.py` (토큰 발급)
- `app/routers/chat.py` (인증 + 파이프라인 통합)
- `app/routers/ingredients.py` / `preferences.py` (인증·경로 정리)
- `app/database.py` (password_hash 컬럼 확장)
- `requirements.txt`, `.env.example`

### 프론트엔드 — 신규(1) / 수정(6)
- 신규: `src/api.js` (토큰 헬퍼 + API_BASE 통합)
- 수정: `App.jsx`, `LoginPage.jsx`, `ChatPage.jsx`(날씨 UI·재료·경고·면책), `FridgePage.jsx`, `HistoryPage.jsx`, `SettingsPage.jsx`

### 문서 — 신규(2)
- `CHANGES_ver4.md` (JWT 보안 작업 기록)
- `PAPER_ROADMAP.md` (논문화 로드맵 — 쉬운 설명·예시·전체 흐름·구현 현황)

---

## 검증 결과

| 항목 | 결과 |
|---|---|
| 단위테스트(양념규칙 제거·flagged·디바이스시간·수동날씨·재정렬) | O |
| 실제 GPT 추천 종단 호출(시간·날씨·재료 필드) | 200 |
| 실제 레시피 호출(6개 상세 섹션) | O |
| 프론트 `vite build` | O |

## 운영 참고 (적용 시 필요)

- `pip install PyJWT` 완료(requirements 반영). `.env`에 **`JWT_SECRET`** 설정 권장(미설정 시 개발용 기본값)
- 서버 재시작 시 `init_db()`가 password_hash 컬럼 확장 자동 적용(확인됨)
- 새 입력 검증은 `ENABLE_SAFETY_FILTER/RERANK/CONTEXT` 플래그 안에서 동작 → baseline 실험 그대로 가능
- 안전 필터는 "위험 감소 + 사용자 확인"이지 의학적 보장이 아님(UI·논문에 명시)
