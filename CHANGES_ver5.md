# 냉털이 — 규칙 기반 하이브리드 추천 파이프라인 (ver5)

> 작성일: 2026-06-02
> 기반 버전: CHANGES_ver4.md (JWT 보안 작업 완료 상태)
> 범위: 백엔드(FastAPI) 추천 로직

---

## 목차

1. [배경](#배경)
2. [전체 동작 흐름](#전체-동작-흐름)
3. [신규/수정 모듈](#신규수정-모듈)
4. [재정렬 점수 공식](#재정렬-점수-공식)
5. [기능 스위치(ablation)](#기능-스위치ablation)
6. [검증 결과](#검증-결과)

---

## 배경

기존 추천은 **메뉴 선정·순위·활용률·기피음식 제외를 거의 전부 GPT가** 처리했다.
즉 "AI에게 잘 물어보는 앱"에 가까웠고, 코드가 직접 만든 알고리즘은 선호 분석 하나뿐이었다.

논문화를 위해 project-improvements.md가 주장하던 **"규칙 기반 + LLM 하이브리드"**를 실제 코드로 구현했다.
핵심은 **GPT는 후보만 생성하고, 우리 코드가 안전 필터·재정렬을 후처리**하는 구조다.

---

## 전체 동작 흐름

```
POST /api/chat/recommend
  ① 신원 확인 (JWT, 게스트 허용)
  ② 입력 수집: 냉장고 재료(로그인 시 DB) + 기피음식 + 선호 카테고리 + 현재 상황
  ③ 프롬프트 생성 → GPT(JSON 모드) 후보 2~3개 생성
  ④ 안전 필터 : 기피·알러지 후보를 코드가 제거 [ENABLE_SAFETY_FILTER]
  ⑤ 재정렬 : 점수 함수로 순위 재결정 [ENABLE_RERANK]
  ⑥ 저장 & 응답
```

---

## 신규/수정 모듈

### 신규 (3)

| 파일 | 역할 |
|------|------|
| `app/services/context_service.py` | 현재 시간대/날씨 등 상황 인식(T1-3) |
| `app/services/safety_service.py` | 기피·알러지 결정론적 하드 필터(T1-2) |
| `app/services/scoring_service.py` | 규칙 기반 재정렬(T1-1) |

### 수정

| 파일 | 변경 |
|------|------|
| `app/config.py` | 기능 스위치(`ENABLE_*`)·점수 가중치(`WEIGHT_*`)·날씨 설정 추가 |
| `app/services/preference_service.py` | `classify_food()`(음식→카테고리), `preferred_categories()` 추가 |
| `app/services/prompt_service.py` | 추천 프롬프트에 상황(context) 정보 블록 추가 |
| `app/routers/chat.py` | 위 ④⑤를 추천 흐름에 통합 |
| `.env.example` | `ENABLE_*`, `WEIGHT_*`, 날씨 키 예시 추가 |

---

## 재정렬 점수 공식

```
최종 점수 = 0.4·활용률 + 0.3·취향일치 + 0.2·상황적합 + 0.1·비용효율
```

- 각 부분 점수는 0~100으로 맞춘 뒤 가중치를 곱해 합산
- 가중치는 `config.py`의 `WEIGHT_USAGE/PREFERENCE/CONTEXT/COST`로 조절 가능
- 같은 입력 → 항상 같은 순위(재현 가능)

예) 추운 비 오는 저녁 + 국물 선호 사용자

| 후보 | 활용률 | 취향 | 상황 | 비용 | 최종 |
|------|:---:|:---:|:---:|:---:|:---:|
| 김치찌개 | 80 | 100 | 100 | 100 | **92** |
| 계란말이 | 90 | 50 | 40 | 100 | 69 |

→ GPT가 계란말이를 위에 줬더라도 코드가 김치찌개를 1순위로 올림.

---

## 기능 스위치(ablation)

`.env`의 플래그만 바꾸면 baseline과 제안 시스템을 코드 수정 없이 전환할 수 있다(논문 실험용).

| 버전 | ENABLE_SAFETY_FILTER | ENABLE_RERANK | ENABLE_CONTEXT |
|------|:---:|:---:|:---:|
| A. 기준선(순수 GPT) | false | false | false |
| B1. +안전필터 | true | false | false |
| B2. +재정렬 | true | true | false |
| B3. 제안 전체 | true | true | true |

---

## 검증 결과

| 항목 | 결과 |
|------|------|
| 안전 필터: 새우 알러지 시 새우 메뉴 제거 | O |
| 재정렬: 추운 날 국물 메뉴 상위 | O |
| 음식 카테고리 분류 | O |
| 전체 모듈 import / 서버 부팅 | O |

> 한계: 이 시점의 안전 필터는 후보 **이름**만 검사하므로 숨은 양념(새우젓 등)은 못 잡는다.
> 이 한계는 ver6에서 보강한다.
